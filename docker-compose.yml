
services:
  backend:
    build:
      context: ./backend
    container_name: flow-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env.prod
    environment:
      NODE_ENV: production
    restart: unless-stopped
